// src/app/designs/a/page.tsx
//
// Variant A — the in-progress homepage direction. Renders inside
// LayoutContent's SiteHeader + PageFrame chrome. The body is the
// shared DesignHomeContent so A and B can be compared like-for-like.

import DesignHomeContent from "@/components/designs/DesignHomeContent";

export default function DesignVariantA() {
  return <DesignHomeContent />;
}
